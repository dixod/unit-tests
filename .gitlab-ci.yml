image: node:22

stages:
  - lint

lint:
  stage: lint
  script:
    - npm ci
    - npm run eslint
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_BRANCH == "unit_tests"'
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "master"'
